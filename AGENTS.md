# Commit messages

When it makes sense, format Git and Jujutsu commit titles as:

```text
<area>: <describe what changed>
```

Use the component being changed as `<area>`. For example, changes to the Fish configuration use `fish`.

The title should describe what changed. Use the commit description to explain why, and how when not trivial. The description should be capitalized.
